---
description: Spawn interactive app orchestrator (manages researcher→coder→verifier pipeline)
---
Read `.agents/agents/cosmos-interactive-orchestrator.md`. Use the Agent tool to spawn an agent with that file's contents as its prompt, targeting: $ARGUMENTS
